{
  "title": "Breed images response",
  "type": "object",
  "required": ["message", "status"],
  "properties": {
    "message": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "format": "uri"
      }
    },
    "status": {
      "type": "string",
      "enum": ["success"]
    }
  }
}
